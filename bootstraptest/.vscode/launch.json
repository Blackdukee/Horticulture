{
  "configurations": [
    {
      "type": "chrome",
      "name": "http://localhost/:80/bootstraptest/${fileBasename}",
      "request": "launch",
      "url": "http://localhost:80/bootstraptest/${fileBasename}"
    }
  ]
}
